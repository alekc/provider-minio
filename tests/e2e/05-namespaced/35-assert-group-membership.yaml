apiVersion: kuttl.dev/v1beta1
kind: TestAssert
timeout: 120
resourceRefs:
  - apiVersion: iam.minio.m.alekc.dev/v1alpha1
    kind: GroupMembership
    name: test-group-membership
    namespace: test-namespaced
    ref: testGroupMembership
assertAll:
  - celExpr: testGroupMembership.status.conditions.exists(c, c.type == "Synced" && c.status == "True")


apiVersion: kuttl.dev/v1beta1
kind: TestAssert
timeout: 120
resourceRefs:
  - apiVersion: iam.minio.m.alekc.dev/v1alpha1
    kind: GroupPolicyAttachment
    name: test-group-policy-attachment
    namespace: test-namespaced
    ref: testGroupPolicyAttachment
assertAll:
  - celExpr: testGroupPolicyAttachment.status.conditions.exists(c, c.type == "Synced" && c.status == "True")


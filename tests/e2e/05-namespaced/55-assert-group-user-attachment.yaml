apiVersion: kuttl.dev/v1beta1
kind: TestAssert
timeout: 120
resourceRefs:
  - apiVersion: iam.minio.m.alekc.dev/v1alpha1
    kind: GroupUserAttachment
    name: test-group-user-attachment
    namespace: test-namespaced
    ref: testGroupUserAttachment
assertAll:
  - celExpr: testGroupUserAttachment.status.conditions.exists(c, c.type == "Synced" && c.status == "True")


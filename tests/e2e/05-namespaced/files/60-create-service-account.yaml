apiVersion: iam.minio.m.alekc.dev/v1alpha1
kind: ServiceAccount
metadata:
  name: test-service-account
  namespace: test-namespaced
spec:
  forProvider:
    targetUserRef:
      name: test-user
    # todo: this is marked as optional but it fails without it.
    # see https://github.com/aminueza/terraform-provider-minio/issues/679
    expiration: 2026-05-24T12:00:00Z
  providerConfigRef:
    kind: ProviderConfig
    name: namespaced-provider


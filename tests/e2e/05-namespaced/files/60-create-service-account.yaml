apiVersion: iam.minio.m.alekc.dev/v1alpha1
kind: ServiceAccount
metadata:
  name: test-service-account
  namespace: test-namespaced
spec:
  forProvider:
    targetUserRef:
      name: test-user
    expiration: 2025-05-24T12:00:00Z # todo: this is marked as optional but it fails without it
  providerConfigRef:
    kind: ProviderConfig
    name: namespaced-provider

